# Changelog

## 0.2.0

- Rename the command and binary from `linkedin` to `post-publisher` (short alias
  `pp`), matching the product name. Release assets are now
  `post-publisher-windows-x64.zip` / `post-publisher-linux-x64.tar.gz`.
- Run CI JavaScript actions on Node.js 24.

## 0.1.0

- Initial scaffold for the Post Publisher CLI package.
- OAuth login plus text, image, and document posting.